
// This file shouldn't be used anymore - redirect to the new auth system
import { AuthContext, AuthProvider, useAuth } from './auth';

// Re-export everything from the new auth context
export { AuthContext, AuthProvider, useAuth };
